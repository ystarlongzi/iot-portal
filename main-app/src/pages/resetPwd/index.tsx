import { FC } from 'react';
import PResetPwd from '@/components/PResetPwd';
import { RouteComponentProps } from 'react-router';

const ResetPwd:  FC<RouteComponentProps> = () => {
  return <PResetPwd />;
};

export default ResetPwd;
